# ShramAI Machine Learning Directory

- `notebooks/`: Exploratory data analysis, feature engineering experiments, and benchmark evaluations.
- `training/`: Repeatable training pipelines for XGBoost, Random Forest, and baseline models.
- `models/`: Serialized model artifacts (`.json` / `.joblib`) and SHAP explainer objects.
