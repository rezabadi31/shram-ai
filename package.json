{
  "name": "shram-ai",
  "version": "0.1.0",
  "private": true,
  "description": "ShramAI: AI-Powered Labour Compliance & Inspection Intelligence",
  "scripts": {
    "build": "npm install --prefix frontend && npm --prefix frontend run build",
    "dev": "npm --prefix frontend run dev"
  }
}
